{
	"name": "{{name}}",
	"description": ">> TODO: Description",
	"version": "0.0.0",
	"license": "MIT",
	"preferGlobal": true,
	"bin": {
		"{{name}}": "./bin/{{name}}"
	},
	"main": "./bin/{{name}}",
	"files": [
		"bin"
	],
	"engines": {
		"node": ">= 12"
	},
	"scripts": {
		"start": "node bin/{{name}}",
		"clean": "shx rm -rf ./dist",
		"depcheck": "depcheck",
		"build": "esbuild --bundle --outfile=bin/{{name}} src/index.{{#if typescript}}ts{{else}}js{{/if}} --target=node12 --platform=node",
		"build:meta": "npm run build --minify --metafile=.meta.json",
		"watch": "npm run build --watch",
		"prod": "npm run build --minify"
	},
	"dependencies": {
		"@snickbit/node-cli": "^1.0.56",
		"@snickbit/node-utilities": "^4.3.13",
		"@snickbit/out": "^2.0.33"
	},
	"devDependencies": {
		"esbuild": "^0.14.38",
		"shx": "^0.3.4",
		"typescript": "^4.6.3"
	}
}
